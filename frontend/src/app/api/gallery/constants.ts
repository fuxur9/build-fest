import { GalleryEvent, GalleryTracks } from "./types";

export const EVENT_DETAILS = {
    event_name: "badger-build-fest",
    event_tracks: {
        blockchain: "Blockchain",
        venture:    "Venture"
    } as GalleryTracks
} as GalleryEvent;